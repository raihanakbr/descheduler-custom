apiVersion: descheduler/v1alpha2
kind: DeschedulerPolicy
metricsProviders:
  - source: KubernetesMetrics
profiles:
  - name: default
    pluginConfig:
      - name: ResourceDefragmentation
        args:
          imbalanceThreshold: 0.20
          maxEvictions: 5
          usageMode: actual-ewma-persisted
          ewmaBeta: 0.90
    plugins:
      balance:
        enabled:
          - ResourceDefragmentation
